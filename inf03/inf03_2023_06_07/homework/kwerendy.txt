kw1
INSERT INTO czytelnicy(imie, nazwisko, kod) VALUES ('Ewelina', 'Romanowska', 56677);

kw2
SELECT imie, nazwisko FROM czytelnicy ORDER BY nazwisko ASC;

kw3
SELECT ksiazki.tytul
FROM ksiazki INNER JOIN autorzy ON ksiazki.id_autor = autorzy.id
WHERE autorzy.nazwisko = "Sienkiewicz"

kw4
SELECT nazwisko, COUNT(tytul) AS ile_tytulow
FROM autorzy JOIN ksiazki ON ksiazki.id_autor = autorzy.id
GROUP BY nazwisko;