kw1
SELECT cena, pojemnosc FROM farby;

kw2
SELECT kolor, cena/pojemnosc AS 'cena jednostkowa' FROM farby;

kw3
SELECT farby.kolor, malowanie.liczba_puszek from farby, malowanie
WHERE malowanie.id_farby = farby.id_farby AND malowanie.id_pomieszczenia = 3;

kw4
CREATE VIEW pomieszczenie1 AS SELECT id_sciany, id_farby, liczba_puszek FROM malowanie WHERE id_pomieszczenia = 1;